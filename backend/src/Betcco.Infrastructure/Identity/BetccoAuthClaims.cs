namespace Betcco.Infrastructure.Identity;

public static class BetccoAuthClaims
{
    /// <summary>Opaque identifier for the server-side browser session record.</summary>
    public const string SessionId = "betcco:session-id";
}
